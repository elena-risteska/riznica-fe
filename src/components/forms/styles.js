const styles = {
  formField: {
    "& .MuiOutlinedInput-root": { borderRadius: 3 },
    "& .MuiFormHelperText-root": {
      minHeight: {
        xs: "14px",
        sm: "16px",
        md: "18px",
      },
    },
  },
  submitButton: {
    width: "80%",
    borderRadius: 4,
    color: "white",
  },
};

export default styles;
